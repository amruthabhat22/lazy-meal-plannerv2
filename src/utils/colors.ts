/** Shared literal colors for props Tailwind classes can't reach. */

/** Placeholder text in inputs — deliberately very light so hints never
 * read as filled-in values (muted-foreground was too close to real text). */
export const PLACEHOLDER_COLOR = "#c9c0b5";
